contract Test {

    uint256 a;
    int b = 0xDEADBEEF;
    string c = "hello world";
    bool d = true;
    int f = b >> 1;

    function main(int x, uint y) public pure returns (uint) {
        return 100 ether;
    }
}